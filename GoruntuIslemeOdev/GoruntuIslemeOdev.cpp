// GoruntuIslemeOdev.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <math.h>
#include <list>


#pragma warning(disable : 4996)
#define PI 3.14159265

int main()
{


	/*ResimNesne::getNesne()->setResimAdi("lanet.jpg");
	ResimNesne::getNesne()->setAnaImg();

	Command *komut = new Command();
	cout << "cikmak icin -1" << endl;
	for (int index = 0; index < komut->getNesneHavuzUzunluk(); index++)
	{
		cout << index + 1 << " = " << komut->getNesneHavuzuEleman(index)<<endl;
	}
	int karar = 0;

	while (true)
	{
		cin >> karar;
		if (karar == -1)
			break;
		komut->calistir(karar-1);
	}

	*/





	getchar();
    return 0;
}

